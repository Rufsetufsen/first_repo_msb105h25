---
title: "Classic R"
format: html
---

```{r}
#Man bør ha ein kodechunk i begynnelsen der me definerer dei ulike packane me skal bruka.
#| label:setup
library(tidyverse)
```

```{r}
df <- data.frame(x = 1:3, y = letters[1:3], z = LETTERS[1:3])
```
```{r}
df
```


```{r}
class(mtcars)
```

```{r}
mtcars <- as_tibble(mtcars, rownames = "Model")
```

```{r}
#| paged.print: false
mtcars[mtcars$mpg < 20, c(1:3, 7)]
```
```{r}
#Får feilmelding. Kvifor?
#| paged.print: false
filter(mtcars$mpg, mpg<20)
```


```{r}
mtcars <-mtcars[, -1]
```


```{r}
#| paged.print: false
mtcars[mtcars$Model %in% c("Merc 280", "Ferrari Dino", "Merc 450SLC"), names(mtcars) %in% c("Model", "mpg", "cyl", "wt")]
```
```

